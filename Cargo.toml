[workspace]
members = [
    "crates/alloc",
    "crates/flipperzero",
    "crates/sys",
    "crates/rt",
]
exclude = [
    "examples/hello-rust",
]

[profile.dev]
opt-level = "z"
panic = "abort"

[profile.release]
opt-level = "z"
panic = "abort"
